# Dev: để trống hoặc comment — frontend dùng /api qua Vite proxy → localhost:5000
# Chỉ set khi build production hoặc backend chạy domain/port khác:
# VITE_API_URL=http://localhost:5000

# Dev: để trống hoặc comment — chat AI dùng /api/v1 qua Vite proxy → localhost:5000
# Chỉ set khi dùng server AI riêng (production):
# VITE_AI_API_URL=http://54.206.118.226:8000
