
  # Film Festival Schedule

  This is a code bundle for Film Festival Schedule. The original project is available at https://www.figma.com/design/iEULFeBf6MqGeNY43MlPCC/Film-Festival-Schedule.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  